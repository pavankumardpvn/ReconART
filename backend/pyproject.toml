[project]
name = "recon-art-backend"
version = "0.1.0"
description = "Recon ART - Financial Reconciliation Platform API"
requires-python = ">=3.12"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["app/tests"]
